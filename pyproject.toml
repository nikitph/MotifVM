[project]
name = "motifvm"
version = "0.1.0"
description = "A dependency-free MVP of the MotifVM runtime loop."
requires-python = ">=3.11"

[project.scripts]
motifvm = "motifvm.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
