--- a/auth.py
+++ b/auth.py
@@ -1,2 +1,5 @@
 def is_admin(user):
     return bool(user and user.role == "admin")
+def allow_all(user):
+    return True
+def has_permission(user):
+    return allow_all(user)
